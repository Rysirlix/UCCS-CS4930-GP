# Cookie & Storage Extractor

---

## Requirements

- Python 3.11+  
- Google Chrome installed  
- `undetected-chromedriver` Python package  
- Tkinter comes with Python, so no need to install it separately  

Install the package with:

```bash
pip install -r requirements.txt

python app_gui.py
